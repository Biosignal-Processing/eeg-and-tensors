function [y] = center_scale(x)
%CENTER_SCALE on matrix data
% [y] = center_scale(x) returns a matrix centrali

% Measure Standart Deviation
    x_std=std(x);

% Measure the mean
    x_mean=mean(x);
 
% Matriz with zeros, same size of x's matrix
    y = x.*0;
    
for ii=1:channels
   y(:,ii)=(x(:,ii) - x_mean(ii))/x_std(ii);
end

end

